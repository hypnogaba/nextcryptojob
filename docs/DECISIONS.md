# Журнал рішень (прийняті без власника, на перегляд)

Формат: дата · рішення · чому · як скасувати.

- 12.09 · На головній лишаємо приклад картки (Engineer, 78, Level 8) з позначкою «Example» · показує продукт з першого екрана · видалити `web/src/components/sample-score-card.tsx` з `page.tsx`.
- 12.09 · Формула v5: Dune (злиті PR у Spellbook) входить у `output`, а не як додаток +10 · варіант з додатком підібраний після перегляду результатів, ризик підгонки · поміняти в `docs/contracts.md` §4 і `engine/src/formula/roles.ts`.
- 12.09 · Sherlock як нова ідентичність (`kind = 'sherlock'`), підтвердження через збіг GitHub або X у профілі Sherlock · єдине відкрите джерело конкурсів аудиту, умови не забороняють · прибрати поле з першого входу.
- 12.09 · Code4rena, Immunefi і профілі Dune не збираємо · їхні умови прямо забороняють автоматичний збір · не скасовувати без юриста.
- 12.09 · Одне рев'ю на задачу замість двох (специфікація + справжні хиби) · вимога швидкості перед запуском · повернути два етапи після запуску.
- 12.09 · Юридичні тексти лишаються чернетками до запуску; три пункти в `docs/BACKLOG.md` · рішення власника «спершу робочий сервіс».
- 12.09 · Сайт тимчасово на `nextcryptojob.hypnogaba.workers.dev`; власний домен після зміни NS у Porkbun · NS міняє лише власник.
- 12.09 · Відповіді на 11 питань специфікації CRM прийнято за пропозиціями: пробний період 14 днів у Stripe з карткою (адмін видає доступ рецензентам і партнерам вручну); квоти 300 пошуків на день, 40 знайомств на місяць, 5 людей у команді; x402 0,50 $ за пошук, 5 $ за знайомство; 100 € на місяць без ПДВ; платні агенти без акаунта можуть лише шукати анонімні профілі; контакт за замовчуванням «після схвалення»; повернення коштів вручну адміном за 7 днів; агенції заповнюють «Hiring for» у кожному знайомстві; вакансії компаній публікуються одразу, адмін може сховати; у релізі 1 лише фасилітатор Coinbase · специфікація `docs/specs/2026-09-12-crm-agents-design.md` §14 · змінити там і в коді.
- 12.09 · Оплата x402 через власну тонку обгортку над `@x402/core` для REST і MCP: `@x402/mcp` несумісний з MCP SDK v2 у пакеті `agents`, а `withX402` від Cloudflare лише для EVM · специфікація CRM §7.
- 12.09 · Solana: гаманець з < 50 успішних транзакцій, перевірений повністю й без обрізання, дає точну кількість обмінів, а не прогалину · повна вибірка точна · договір §3.
- 12.09 · Картку ролі можна створити, лише коли головне джерело ролі підтверджене (GitHub кодом у біо для інженера, аудитора, DevRel; X кодом для медійних ролей); YouTube-креатори поки без картки; картка трейдера має позначку «Wallets not verified», поки немає підпису гаманця · інакше можна поширити фальшиву картку з чужим GitHub · web: правило створення картки.
- 12.09 · Рушій підтверджує Sherlock лише через підтверджені GitHub або X · захист від привласнення чужих заробітків аудитора.
- 12.09 · Власник ніка може забрати його в неперевіреного «загарбника», підтвердивши кодом; ретвіти не рахуються як підтвердження · рев'ю W4.
- 13.09 · Без налаштувань x402 (немає CDP_API_KEY_* або адрес отримувача) платна дія відповідає not_configured з назвами змінних: гість 401 (специфікація 7.3), компанія з чинним ключем 503 (як NotConfigured в openapi.yaml), фасилітатора не питаємо · ключ компанії правильний, 401 збивав би агента з пантелику · змінити статус у `web/src/lib/crm/execute.ts`.
- 13.09 · Ідемпотентний повтор оплаченого пошуку збирає відповідь з рядка журналу `candidate.search` цього платежу (id кандидатів і курсор), кандидати знову проходять правило видимості; профілі в базі не зберігаємо · пошук удруге не виконується (§17), а знімок профілів жив би довше за згоду людини · `replaySearch` у `web/src/lib/crm/search.ts`.
- 13.09 · REST без ключа й без платежу на пошуку кандидатів дає 402 (а не 401), щоб клієнт x402 дізнався ціну; решта маршрутів без ключа 401 · так працює транспорт HTTP x402 · `web/src/lib/api/rest.ts`.
- 13.09 · Окрема прив'язка RL_PUBLIC (30/хв на IP) для публічного search_jobs, бо RL_IP має 10/хв для гостей x402 · специфікація розділ 9 дає різні межі · `web/wrangler.jsonc`.
- 13.09 · MCP на хибний чи відкликаний ключ відповідає HTTP 401 з `WWW-Authenticate: Bearer realm="nextcryptojob", error="invalid_token", error_description=…`, без `resource_metadata` · перевірено в `@modelcontextprotocol/sdk@1.30` (клієнт Claude Code, Claude Desktop, Cursor): з увімкненим OAuth клієнт на будь-який 401 сам пробує `/.well-known/oauth-protected-resource`, навіть без `resource_metadata` і з іншою схемою (`extractWWWAuthenticateParams` читає лише Bearer, інакше порожньо), отримує 404 і показує помилку входу; заголовок цього не вимикає, вимкнув би лише інший статус (403), а він суперечить REST і договору ключа (401 key_revoked) · змінити в `web/src/lib/api/mcp.ts`.
- 13.09 · «Paid without result» зберігаємо колонками `x402_payments.no_result_at/no_result_reason` (+ `refunded_*`), а не новим станом `settled_no_result` · CHECK стану не змінити без перебудови таблиці, а DROP TABLE при ввімкнених зовнішніх ключах обнулив би `usage_events.x402_payment_id`; міграція 0016 · `db/migrations/0016_x402_no_result.sql`.
- 13.09 · Повтор оплаченого запиту, для якого результату ще немає: 409 «retry» 5 хв після settle, далі платіж стає «Paid without result» (500) · процес міг упасти між settle і записом, і вічний 409 сховав би це від адміна · `IN_FLIGHT_MS` у `web/src/lib/crm/execute.ts`.
- 13.09 · Ліміт сплесків REST і MCP іде до тіла запиту й до D1: ключ рахується за SHA-256 самого ключа, без ключа за IP (MCP: кожен запит, і initialize, і tools/list, у RL_PUBLIC; платний інструмент гостя ще й у RL_IP) · зайвий запит не має коштувати читання D1 · `checkRequestBurst` у `web/src/lib/crm/quotas.ts`.
- 13.09 · Cron (T11): третій тригер `0 3 * * *` для щоденного прибирання (сесії, коди входу, лічильники, апдейти бота, облік старший за 400 днів) замість перевірки години всередині щогодинного · кожен тригер має свій перелік задач без годинникової логіки · `CRONS` у `web/src/lib/cron/index.ts` і `triggers.crons` у `web/wrangler.jsonc` (тест звіряє).
- 13.09 · Журнал доставок вебхука живе в `audit_log` з актором `<company_id>:webhook` (рядок на кожну спробу й тестовий ping), нової таблиці немає (рішення 10 специфікації); Activity log цих рядків не показує, їх показує сторінка Developers · індекс (actor, at) дає журнал компанії без чужих рядків · `web/src/lib/crm/webhooks.ts`.
- 13.09 · Адреса вебхука: лише https на порту 443, публічне доменне ім'я (та сама перевірка, що для сайту людини: `isPublicHostname`), без логіна в адресі, не наш домен і не наш піддомен workers.dev (`OWN_HOSTS`, тест звіряє з wrangler.jsonc); переходи 3xx не виконуємо; DNS не перевіряємо (у Worker хибні відмови); адреса перевіряється ще раз перед кожною спробою. Приватні адреси недосяжні, бо в Worker немає приватної мережі; `global_fetch_strictly_public` лише не дає fetch на нашу власну зону піти в обхід публічного шляху · `webhookUrlProblem`.
- 13.09 · Ключ HMAC підпису вебхука = UTF-8 байти всього секрету разом з `whsec_` · так роблять Stripe і приклад на сторінці Developers · змінити в `signatureHeader` і прикладі.
- 13.09 · Перша спроба вебхука одразу після відповіді кандидата (`ctx.waitUntil`, `webhook-kick.ts`), решта і прострочення через cron кожні 5 хв; вимкнений на мить доставки вебхук знімає подію з черги (`webhook_state = 'none'`) · специфікація 7.6 · `deliverIntroWebhook`.
- 13.09 · Cron після рев'ю T11: `scheduled` чекає runCron сам (await), не через `ctx.waitUntil` (його обривають приблизно за 30 с після виходу з обробника, а запуск звітує ok); межа часу на тригер (`CRON_BUDGET_MS`: 4,5 хв для 5-хвилинного, 10 хв для щогодинного й щоденного) і на задачу; вебхуки: свій час на кожну доставку (оренда, `t` підпису, запис), не більше 10 подій компанії за запуск, після першого тайм-ауту компанію до кінця запуску пропускаємо, нових рядків після 4 хв не беремо · `web/src/lib/cron/index.ts`, `deliverWebhooks`.
- 13.09 · Сповіщення збережених пошуків: спершу надсилаємо, потім позначаємо (щонайменше раз): запуск, що впав між ними, повторить сповіщення наступної години, а не загубить його; щогодинні запуски не перетинаються завдяки межі 10 хв · `savedSearchAlerts`.
- 13.09 · Сповіщення збережених пошуків: `last_alert_at` = остання щоденна перевірка (без нових `last_match_count = 0`, сторінка пише "Checked … no new candidates"); нові = збіги, чия видимість чи бал змінились після max(baseline_at, last_alert_at), і яких ще немає в `seen_json` (щотижневий перерахунок балу не повторює людину); часового поясу компанії немає, тож UTC і рівно 24 год від запланованої години cron · інакше пошук без нових перевірявся б щогодини · `web/src/lib/cron/alerts.ts`.
- 13.09 · Договір API віддає сам сайт: `/openapi.yaml` (копія `docs/api/openapi.yaml` у `web/public`, оновлює prebuild, тест звіряє байти), бо репозиторій закритий, а сторінка Developers має посилання на документацію · `web/scripts/api-docs.mjs`.
- 13.09 · Ворота якості релізу 1 (власник обрав варіант A): `passed` = у межах сусіднього рівня ≥ 85%; промахи на 2 рівні лише метрика в `report_json` і список у `docs/BACKLOG.md` · еталон це 49 людей з рівнями, розставленими вручну; 5 промахів на 2 рівні без прогалини в даних, усі поза роллю трейдера й однакові у v5 і v6, а через другу умову компанії не бачили жодного балу · повернути другу умову («жодного промаху на 2 рівні без прогалини») в `engine/src/pipeline/quality-gate.ts` (`evaluateGate`), `docs/contracts.md` §4 і `docs/specs/2026-09-12-release1-design.md` §6.3.
- 14.09 · Рішення власника: NextCryptoJob і NextRole розділено повністю. Свій крипто-сканер у engine (`src/jobs`) і своя база вакансій D1 `nextcryptojob-jobs` (`db/jobs`); сайт і добірка читають лише її; реєстр роботодавців засіяно один раз з публічних даних NextRole (`db/jobs/seed`), далі живе сам · спільна база зв'язувала два продукти: розклад, збої й записи одного били по іншому · скасувати: повернути binding `JOBS_DB` і змінну добірки на стару базу разом із попереднім кодом.
- 14.09 · Сканер ходить щодня, і у вихідні теж, о 04:30 UTC · пул живих вакансій (бачені за 3 доби) не худне на вихідних; 04:30, щоб не збігатися з іншим сканером на тій самій машині о 03:00 · `engine/deploy/nextcryptojob-jobs-scan.timer` і `SCAN_TIME_UTC` у `web/src/lib/admin/job-sources.ts` разом.
- 14.09 · Розвідка з колекцій Getro вимкнена (`JOBS_GETRO_DISCOVERY=0`), Superteam Earn вимкнений (`JOBS_SUPERTEAM=0`), cryptocurrencyjobs.co і crypto-careers.com не читаємо · умови Getro, cryptocurrencyjobs і crypto-careers забороняють автоматичний збір; Superteam дає баунті без зарплати · увімкнути змінною в `/etc/nextcryptojob-engine.env` (Getro лише рішенням власника).
- 14.09 · Рішення власника: дошки екосистем і фондів (Solana, cyber.fund, Arbitrum, Multicoin, Dragonfly та ще 21 на Getro) читає щотижнева розвідка (`JOBS_GETRO_DISCOVERY=1` на VPS): список компаній дошки, а для компанії, якої реєстр не знає, одна сторінка її вакансій лише заради адреси ATS; вакансії йдуть з API ATS роботодавця, тексти Getro ніде не зберігаються, щоденний скан Getro не читає. Дошки Consider (a16z crypto, Paradigm, Pantera, Hashed, Lemniscap, Fenbushi) не читаємо ніколи; роботодавців з публічних сторінок портфелів додано руками (`discovered_via = 'portfolio:<фонд>'`). Реєстр дошок `job_boards` (`db/jobs/0003_job_boards.sql`, `db/jobs/seed/boards.json`) · власник прийняв ризик умов Getro заради малого обсягу (кілька сотень запитів на тиждень, 1 на 1,5 с) · вимкнути: `JOBS_GETRO_DISCOVERY=0` або `decision = 'skip'` у рядку дошки.
- 14.09 · `jobs_cache` у новій базі WITHOUT ROWID, id = хеш адреси, без вторинних індексів; стан джерела пишеться лише при зміні · записи D1 у тисячу разів дорожчі за читання, а скан щодня переписує `fetched_at`: 1 записаний рядок на вакансію · `db/jobs/0001_schema.sql`.
- 14.09 · Мітки `sent.source = 'nextrole'` і `sent.job_ref = 'nr:…'` лишаються для вакансій зі сканування · CHECK у 0006 і контракт листа engine → web; зміна потребувала б перебудови `sent` і одночасного деплою обох · перейменувати міграцією разом зі схемою листа.
